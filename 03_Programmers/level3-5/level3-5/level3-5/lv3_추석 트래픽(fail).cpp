#include<iostream>
#include <string>
#include <vector>

using namespace std;

int solution(vector<string> lines) {
	int answer = 0;
	return answer;
}
int main() {
	freopen("2.in", "r", stdin);
	vector<string> l;
	for (int i = 0; i < 10; i++) {
		string s;
		cin >> s;
		l.push_back(s);
	}
	solution(l);

	return 0;
}